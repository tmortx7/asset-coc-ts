query AllSites($limit: Int!, $cursor: String) {
  allSites(limit: $limit, cursor: $cursor) {
    hasMore
    allSites{
      id
      createdAt
      updatedAt
      sitename
      sitenumber
      description
      note
    }
  }
}