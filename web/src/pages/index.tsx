import Layout from '../components/layout';
import { GridItem } from '@chakra-ui/react';

const IndexPage = () => (
  <Layout>
    <GridItem colStart={6} rowStart={2}>hello world</GridItem>
  </Layout>
);

export default IndexPage;
